// 결과예측하기 버튼에 클릭 이벤트 리스너를 추가
// HTML문서에서 id가 'predict-button'요소를 선택
document.getElementById('predict-button').addEventListener('click', function() {
    //results라는 객체
    // : LG와 KT 각각의 승리 횟수를 추적
    let results = {"LG"}
});